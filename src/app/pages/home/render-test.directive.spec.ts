import { RenderTestDirective } from './render-test.directive';

describe('RenderTestDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderTestDirective();
    expect(directive).toBeTruthy();
  });
});
