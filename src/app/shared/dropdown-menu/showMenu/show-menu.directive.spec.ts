import { ShowMenuDirective } from './show-menu.directive';

describe('ShowMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowMenuDirective();
    expect(directive).toBeTruthy();
  });
});
